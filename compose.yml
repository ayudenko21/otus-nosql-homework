services:
  app:
    build:
      context: .
    container_name: app
    volumes:
      - ./:/app

  redis-otus:
    image: redis:latest
    container_name: redis-otus
    ports:
      - 6380:6379

networks:
  default:
    name: default-otus
